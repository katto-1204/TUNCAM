{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "artifacts/tuncam"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/tuncam",
    "build": "npm run build --workspace=@workspace/tuncam",
    "typecheck": "npm run typecheck --workspace=@workspace/tuncam"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
